node_modules/
package-lock.json

# Capture output
*shots/
cmp_*/
clip/
assets/.tmp/

# Third-party textures fetched from activetheory.net at runtime.
# These are Active Theory's assets — not redistributed here.
# Run `npm run fetch:assets` to pull them locally.
assets/at/

# Source GLB is 69.78 MB, over GitHub's file-size warning threshold.
# The compressed builds (spine.opt.glb 2.77 MB / spine.min.glb 1.18 MB) are
# committed and are what the app loads. Keep the source locally to re-run
# `node compress.mjs`.
assets/spine.glb

.DS_Store
Thumbs.db
