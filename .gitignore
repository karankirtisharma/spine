node_modules/
package-lock.json

# Capture output
*shots/
glitchshots/
cmp_*/
clip/
assets/.tmp/

# A sweep capture, regenerated by test/sweep.js on every verification run. The
# BASELINE it is compared against (test/baseline.json) is committed, because that
# one is the reference the Work section is pinned to.
test/after.json

# Active Theory's own assets, pulled by `npm run fetch:assets`.
#
# The folder is ignored by default, then the three files the app actually loads
# are un-ignored below so the deployed build matches local. Without them it falls
# back to the procedural cloud and procedural textures, which looks materially
# worse -- the floret cloud in particular is most of the look.
#
# Only those three are committed; the rest of the folder stays out. They remain
# Active Theory's work and are credited in the README.
# `assets/at/*`, not `assets/at/` -- git cannot re-include a file whose PARENT
# DIRECTORY is excluded, because it never descends into it, so the negations
# below would silently do nothing. Excluding the contents keeps the directory
# itself visible and lets the exceptions apply.
assets/at/*
!assets/at/flower_spine-512.bin
!assets/at/env1.jpg
!assets/at/waternormals.jpg
# matcap-test.jpg is the faceted-crystal matcap uil.json binds to JellyShader as
# both tMap and tMatcap. It is what makes the jellyfish read as three-dimensional
# glass rather than a tinted silhouette, so the deployed build needs it.
!assets/at/matcap-test.jpg
# alien_cracked_2_normal.png is uil.json's tNormal on JellyShader -- the cracked
# organic membrane that keeps the bell from reading as a smooth CG dome.
!assets/at/alien_cracked_2_normal.png
# jellyfish.bin is their ACTUAL jellyfish model, assets/geometry/home/jellyfish.bin,
# 15,672 verts of Draco in 24 KB. Every procedural reconstruction of this creature was
# wrong in a different way; there is no sensible fallback for it, so it is committed.
!assets/at/jellyfish.bin
# jungle_soil_normal.png is AboutLogoShader's tNormal -- the screen-space relief that
# gives their logo glass its liquid distortion. The coin shader binds it.
!assets/at/jungle_soil_normal.png
# matcap3.png is the bubble sprite their LogoParticleShader and TreeParticleShader
# both bind as tMap; the foliage instances sample it.
!assets/at/matcap3.png
# tree-256.bin is their baked tree point cloud (offset+random attributes) -- the
# second foliage silhouette in the density pass.
!assets/at/tree-256.bin
# Their environment set: tree_room/room meshes, the light-volume plates and the
# area-light table, fetched by fetch:assets. The directory contents are committed
# because the deployed build needs the walls and the light pools.
!assets/at/env
!assets/at/env/**

# Source GLB is 69.78 MB, over GitHub's file-size warning threshold.
# The compressed builds (spine.opt.glb 2.77 MB / spine.min.glb 1.18 MB) are
# committed and are what the app loads. Keep the source locally to re-run
# `node compress.mjs`.
assets/spine.glb

# Emblem source, 53.36 MB. The compressed build (emblem.opt.glb, 2.66 MB) is
# committed and is what the app loads. Keep the source locally to re-run
# `node compress.mjs emblem`.
assets/emblem.glb

.DS_Store
Thumbs.db
.superdesign

# Local agent/skill config, not part of the project
.claude/

# local capture output from Capture-Frames.ps1
framames/
frames/

# original 1920x1080 film plate -- superseded by assets/deep-bg-960 (see
# filmseq.js PATH). Kept on disk as the master; not needed to run the site.
assets/deep-bg/

# audit harness output
test/out*/
